namespace WorkflowPro
{
    // Bootstrap 5 (CDN) and site.css/site.js (/Content, /Scripts) are referenced
    // directly from _Layout.cshtml to avoid pulling in the System.Web.Optimization
    // package, per the "keep it simple" requirement. Nothing to register here.
    public class BundleConfig
    {
        public static void RegisterBundles()
        {
        }
    }
}
